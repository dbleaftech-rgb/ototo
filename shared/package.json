{
  "name": "@ototo/shared",
  "version": "1.0.0",
  "description": "Shared domain models, types, scoring and valuation algorithms for Ototo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
